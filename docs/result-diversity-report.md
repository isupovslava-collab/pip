# Отчёт по разнообразию результатов Sprint 7

Дата проверки: 2026-08-02. Команда: `npm run report:diversity`.

Проверено 16 новых запросов и 16 пар существенно разных запросов из `src/data/relevanceQueries.ts`. Для каждого запроса подтверждены детерминированность Top 6, минимум три `compositionFamily`, максимум две карточки одного `compositionFamily` и максимум две одного `visualDirection`.

| Метрика | Результат | Цель | Статус |
|---|---:|---:|---|
| Пар запросов | 16 | ≥16 | PASS |
| Среднее пересечение Top 6 | 0,06 | ≤2 | PASS |
| Максимальное пересечение Top 6 | 1 | ≤2 | PASS |
| Средний Jaccard | 0,006 | информационно | PASS |
| Пары с одинаковым Top 3 | 0 | 0 | PASS |
| FAIL pairs | 0 | 0 | PASS |

Измерение не использует случайность и повторяется в тесте `src/tests/relevanceDiversity.test.ts`. Низкое пересечение получено семантическими метаданными и мягким reranking, а не исключением карточек из библиотеки.
